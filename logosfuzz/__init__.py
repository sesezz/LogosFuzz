"""LogosFuzz — LLM 기반 자동차 오픈소스 특화 퍼징 하네스 자동 생성 시스템.

파이프라인: EXT(extract/knowledge) -> SCH(schedule) -> GEN(generate)
            -> EXE(execute) -> ANA(analyze), 제어 계층은 CTR(control).
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
